# BuildSignal v1.0

Cloudflare Pages deployment repository.

## Deployment

This repository is connected to Cloudflare Pages for automatic deployment.

## Build

Cloudflare Pages should run:

```bash
npm install
npm run build
```

The SPA source lives in `/src` and is built with Vite into `/dist`.
